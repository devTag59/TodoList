import React from 'react';
import { StyleSheet, View } from 'react-native';

export interface User {
    Nome: String,
    senha: String,
}

const User = () => {
    return (
        <View>
            
        </View>
    );
}
export {User};
